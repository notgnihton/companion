# GitHub Authentication
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories) or issues:write
GITHUB_TOKEN=ghp_your_token_here

# Repository to test on (format: owner/repo)
GITHUB_REPO=lucyscript/companion

# Issue number to test Claude assignment on
# If not set, defaults to issue #1
TEST_ISSUE_NUMBER=1
